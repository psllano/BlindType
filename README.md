# BlindType

This repository contains the BlindType project.

Contents:
- `blindtype.html` — demo page
- `to_txt.py` — helper script

Usage:
1. Install Python dependencies if needed.
2. Run `python to_txt.py` or open `blindtype.html` in a browser.

License: add a license if desired.
